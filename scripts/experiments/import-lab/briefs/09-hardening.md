# Task: make the import lab a durable evaluation tool

You are a coder with one bounded task. Not an orchestrator. Do not commit, stage, push or branch. No internet, no paid calls, never read `.env*`. Work only in `scripts/experiments/import-lab/`; no production code changes. Real data under `.import-lab/` may be READ (never copy real site names, URLs or page text into code, tests, fixtures, README or briefs). Saved runs, labels and scores must stay readable - never delete or rewrite them.

The owner's words: the tool is good and must stay in good shape so the same audit can be re-run in future evaluations, "even the prompt so we can re-generate it". The owner is a non-developer with ADHD: every output they read must be short, plain-language, and lead with the answer.

## 1. One short summary report (most important)
The existing reports are unusable for a person (`comparison.md` is 1.1 MB, `phase1.md` 1.7 MB, `comparison.json` 243 MB). Add `summary.ts` writing `.import-lab/reports/SUMMARY.md` (under 150 lines) and `summary.json`, built ONLY from saved scores/runs (no model calls):
- first line: one sentence with the headline (component right: today vs best arm, with counts);
- the arm table: per arm and run - correct / right component, content incomplete / wrong component / missed / component right (count and share), with the number of blocks; arms discovered from what is saved, not hard-coded;
- the same split into held-out pages and other pages (see 3);
- decision-model picking: blocks, top choice acceptable, any of top three acceptable, the table by stated probability band, the ten most frequent confusions;
- stability: for every arm with two or more runs, blocks with the same verdict in both;
- per arm: pages, median and range of page wall-clock seconds, calls, infrastructure failures (timeouts, truncated replies), total / reasoning tokens where recorded, cost per page and total;
- which content checks fail most, per arm;
- label provenance: how many scored blocks were reviewed by whom (`reviewedBy` values);
- a fixed "what this cannot show" section (number of pages, page kinds, one extraction model, no visual check);
- plain words only; every share shows its counts; figures resting on fewer than 20 blocks are flagged.
Make the giant reports opt-in: `report.ts` writes them only with `--full`; by default it writes the summary. `comparison.json` must not be regenerated by default.

## 2. Fix the slow block preparation
On one large real page, preparing block inputs takes more than 15 minutes with no model call (a dry run of `run-arm.ts --arm oracle-type-today-rules --dry-run` on that page did not finish in 900 s; smaller pages take ~4 s). The alignment of block nodes against saved production nodes is the suspect. Profile it offline on the real saved pages, fix the algorithm (index saved nodes once per page; avoid quadratic rescans; cache per page across blocks), keep results identical (add a test asserting identical block inputs before/after on a fixture, and compare on real pages offline), and report before/after seconds per real page by slug.

## 3. Page set manifest
Add `.import-lab/pages.json` support (git-ignored data, create it from the current pages): for each page slug - `url`, `kind` (home, landing, article, listing, form, other), `heldOut` (true for pages that must never inform design decisions), `renderWithJavaScript` (the two script-heavy pages were proposed with `--no-js`), `notes`. `propose-blocks.ts` honours `renderWithJavaScript`; `summary.ts` uses `heldOut`. Currently held out: four page slugs, two of which render without JavaScript (the orchestrator supplied the slug prefixes when running this brief; they name real sites and are recorded only in the git-ignored `.import-lab/pages.json`). Document the file in the README; ship an invented `pages.example.json` beside the code.

## 4. One runbook and one command per stage
Add `RUNBOOK.md`: how to run a complete evaluation from nothing, in order, each step marked FREE / INTERNET / PAID with a typical cost and duration taken from the saved runs: add pages -> snapshot -> today's detection (x2) -> repair arms + measure -> propose blocks -> draft labels -> review -> score -> comparison arms (with the retry flags) -> summary. State the environment needed by variable NAME only (including `IMPORT_LAB_CHROMIUM_EXECUTABLE`), the known provider behaviours (calls that hang, credit exhaustion error 402 and how a draft run is resumed for only the failed blocks - add `--only-failed` to `draft-labels.ts` if it does not exist), and how to add a NEW arm (the contract: write `components.json` + `run.json` + `calls/` under `.import-lab/arms/<slug>/<arm>/<run>/`, then `score.ts`).
Add `eval.ts` with sub-commands that loop the existing scripts over every page in `pages.json` with bounded parallelism and resume-by-skipping-existing: `eval.ts today --runs 2`, `eval.ts blocks`, `eval.ts arms --arms a,b,c --run r1 [--stall-timeout-ms --infrastructure-retries]`, `eval.ts score`, `eval.ts summary`. `--dry-run` prints the plan, call counts and an estimated cost from saved history. No paid call without an explicit `--yes-spend`.

## 5. Keep the build prompts reproducible
`briefs/` now holds the nine briefs (this is number 9) that produced this tool, with site names removed. Add `briefs/README.md` explaining: what each brief built, the order, that they were given to a coding agent with no internet and no secrets, which real-page findings triggered each fix round, and how to reuse them to regenerate or extend the tool. Do not alter the brief texts.

## 6. Small known defects
- `review-server.ts`: refuse to start with a clear message when the port is already in use (today a second instance fails silently while the old one keeps serving).
- `score.ts --all-runs` exits non-zero because of dry-run folders: skip runs whose `run.json` status is `dry-run`.
- Expected item counts drafted from a screenshot are wrong for carousels (visible slides vs slides in the HTML). In `draft-labels.ts` give the drafter the number of repeated child structures from the saved HTML and ask for the count in the HTML; in `scoring.ts` report item-count mismatches separately so they can be excluded from the verdict with `--ignore-item-count`.

## Tests and verification
Unit tests for summary maths (shares, stability, held-out split, bands), manifest handling, `eval.ts` planning in dry-run, port-in-use, the dry-run skip, `--only-failed`. `SKIP_DB_SETUP=true npx jest scripts/experiments/import-lab --runInBand` passes (set `IMPORT_LAB_CHROMIUM_EXECUTABLE` to the path in the README for browser tests); `npm run typecheck` reports 0 errors. Then run `summary.ts` offline against the real saved data and paste the first 25 lines of `SUMMARY.md` into your final message (slugs may appear there; that file is git-ignored).

## Final message
Plain English: what changed, before/after preparation seconds per page, the first 25 lines of the real summary, anything not done.
