- Source import: `_output.txt`, runId `50d855b9-a95e-413d-a93e-782409acacd1` (2025-10-29T22:56:14Z); refreshed 2025-10-31 after updating the prompt contract so detectors omit `content.region`/`summary`.
- Landing hero now maps to the canonical `hero-with-image` payload (Coffee With a Cop) instead of injecting a fallback carousel; cta buttons retain contract `variant`/`href` fields.
- Both `card-grid.cards[]` collections keep stable IDs, trimmed descriptions, and per-card metadata; non-contract container fields (`heading`, `subheading`, `columns`) were dropped per prompt guidance so importer diffs stay at zero.
- `cta-with-form` returns heading, copy, placeholder, button text, and privacy link; the live DOM still omits `formAction`, `emailFieldName`, `successMessage`, and honeypot inputs (verified 2025-10-31), so those contract fields remain absent.
- Footer columns/legal/social links arrive from the detector in contract shape and social IDs follow the `socialLinkItem-<slug>` pattern; newsletter is not rendered on this page.
- Fixture strict-passes `pnpm run eval:detection run --dataset bathurst --fail-on-importer-fix` (latest report: `reports/eval/detection/2025-10-31T04-30-53-391Z.json`). Keep using it to guard hero/card/footer regressions as importer helpers are retired.
- Regenerate flow: `pnpm run eval:detection run --dataset bathurst --report --fail-on-importer-fix --record`, promote the freshest `raw.json`, then `pnpm exec tsx scripts/eval/build-expected-from-raw.ts prompts/evals/bathurst/home/raw.json prompts/evals/bathurst/home/expected.json`.
