# Brief M2b: pick the component per block, fill it, and wire the `blocks` harness behind the existing setting

You are a coder with one bounded task. You are not an orchestrator: do not run any engine, preflight or front-door tooling. Do not commit, stage, push or branch. No internet and no paid calls from anything you run (fake clients only). Never read `.env*` files (editing `.env.example` is allowed). Read `docs/import/block-detection-plan.md` first - decisions D1, D2, D5-D10 and milestone M2 are binding and not open for redesign. M2a (committed, under `lib/studio/import/detection/blocks/`) gives you `renderAndCut`, `cutRenderedPage`, `buildBlockInput({ html, block, bgImageMap })` and `BlockCutError`; the page's styling map is read from the web tools by handle (the accessor M2a added to `lib/studio/import/services/web-tools.ts`). Read those files before writing anything.

The owner's standing rules: no over-engineering, no slop, deliver exactly what is asked - no more, no less; clean up after yourself; no dead code; as simple as possible. Match the style, naming and comment density of the surrounding production code. No new dependency. No new setting beyond the two named below. No abstraction with a single user. Prefer a function to a class. If you need a production function that is not exported, add `export` to it - never copy production logic into a new file. Do not export anything nothing imports. One statement per line, in the style of the neighbouring production files - the lab's dense one-line style does not belong in `lib/`.

Memory discipline (this machine ran out of memory during an earlier task): at most one Node process at a time, Jest always with `--runInBand --forceExit` and one path at a time, never a watch mode, and leave no Node or Chromium process running when you finish.

## Reference code to port from (do not import from `scripts/` in production)
The lab's `picks.ts` (question wording, evidence rendering, the allowed-types rule), `extract-block.ts` + `arm-runtime.ts` (single-block prompt from production's own builders for a given list of allowed types, the call, infrastructure retry, validation repair) and `run-arm.ts` (`jev-then-llm-blocks`: how a page is run). These were measured on real pages: 65-68% of blocks with the right component in 10-30 seconds per page. Port that behaviour; do not improve it.

## Build
1. `lib/studio/decisions/questions/import-block.ts`: two questions registered the way the existing ones are - `import.block.component@1` (choice; options = every page-level component type with its catalogue summary, the same filter production's `candidate-types.ts` uses to exclude sub-components) and `import.block.multiple@1` (yes/no: does this block contain more than one component?). Both asked in ONE request per block through the existing panel call. Evidence = the rendered outline of the block as in the lab (headings, counts of images / links / buttons / repeated items, indented outline with truncated text), truncation recorded.
2. `lib/studio/import/detection/blocks/block-pick.ts`: `pickBlockTypes(blockInput)` returns the allowed types and the raw answer. Rule D6, fixed constants, no settings: top choice at least twice the second -> that one type; otherwise the top three; "multiple" at least 0.5 -> the top three. D7: if the decision call fails or times out for a block, allowed types = what production's own candidate selection offers for that content (the same functions `web-detection.ts` uses for a section), and the recorded source says so.
3. D7 start check: when the harness is `blocks` and the decision model is disabled or in shadow mode (`lib/studio/decisions/config.ts`), detection throws before any work with one message naming `DECISION_MODEL_ENABLED` and `DECISION_MODEL_SHADOW`. Note that `ask()` returns the fallback in shadow mode - make sure the live answer is what `block-pick.ts` uses, in the simplest way the decisions module allows, without changing behaviour for the existing questions.
4. `lib/studio/import/detection/blocks/block-extract.ts`: `extractBlock({ blockInput, allowedTypes, pageOutline })` builds the prompt with production's existing builders for ONLY the allowed types (today's rule text - no "full rules" variant), calls the fill model, validates with `parseSectionDetectionResponse` exactly as the section path does, one validation-repair retry as today. Infrastructure retry: a call silent for 120 seconds or a reply that is cut off is re-sent unchanged, up to 2 more times (constants). The one-line page outline (block number, region, picked type for every block) is included as context, as in the lab.
5. Settings in `lib/studio/import/config/import-config.ts`, in the file's existing style: widen `detectionHarness` to include `'blocks'` (default stays `'section'`); `IMPORT_BLOCK_FILL_MODEL` default `inception/mercury-2.5`; `IMPORT_BLOCK_CONCURRENCY` default 8. Add the three to `.env.example` with one-line comments. Nothing else.
6. `lib/studio/import/detection/blocks/block-harness.ts`: `runBlockHarness(...)` - cut the page (M2a), pick all blocks, extract all blocks with the concurrency limit, and return the SAME `SectionProcessingResult[]` the other two branches return, so the shared tail of `web-detection.ts` (aggregation, head metadata, page template, checkpoints, progress, telemetry, cost) runs unchanged. Each block is one task in the page plan with key `block:<order>` and its region as role; `savePagePlan`, `saveSectionResult`, `saveSectionError`, resume from checkpoint and `onProgress` work as they do for sections. Header and footer blocks use the existing per-import reuse cache. D9: a block that still fails is dropped and reported through the existing section-error path; a page with zero surviving components still fails.
7. `lib/studio/import/web-detection.ts`: add the third branch at the existing harness switch (near line 1805). The two existing branches must stay textually unchanged. `lib/studio/import/detection/section-aggregation.ts`: the "header must yield a navigation bar / footer must yield a footer" requirement is evaluated per region when tasks are blocks - make the smallest change that does this without altering results for section tasks.
8. D10: `ImportDetectionResult` carries the harness name; `lib/studio/import/services/import-result-handler.ts` (the `adjustDetectedComponents` call near line 548) skips the repair step when it is `blocks`. Conditioned on that field only.
9. D4: a `BlockCutError` fails the page import with its message in the diagnostics. No fallback to another harness.

## Tests (fake model and decision clients, fixture html, no network)
- Setting unset -> `section`; and on the `section` path no module under `detection/blocks/` is loaded and no browser launch happens (spy on the launcher).
- With `blocks`, a fixture page imports end to end: components in block order, header/footer roles satisfied per region, page metadata present.
- One block's call times out then succeeds; one reply is cut off then succeeds; one block exhausts retries and is dropped with a diagnostic while the page survives; zero surviving components fails the page.
- Decision call fails for one block -> production candidate types are offered and the source is recorded.
- Shadow mode or disabled decision model with `blocks` -> the start error.
- Unstyled render / launch failure -> the page fails with the `BlockCutError` message.
- Resume: a run interrupted after some blocks were checkpointed finishes without re-calling them.
- The repair step is skipped for a `blocks` result and still runs for a `section` result.
- The allowed-types rule: all three branches.

## Out of scope (do not do)
Changing prompts' wording, the component catalogue, the decisions client or endpoint, the lab, deleting the old path (that is milestone M6), new CLI flags, README rewrites, telemetry beyond what the shared tail already records.

## Verify yourself
`npm run typecheck` 0 errors; `SKIP_DB_SETUP=true npx jest lib/studio/import/detection lib/studio/decisions --silent` - no new failures (baseline before M2: detection 124 tests with 4 failing in `response-parser.test.ts`; record the decisions baseline before you start); `SKIP_DB_SETUP=true npx jest lib/studio/import/__tests__/web-detection.test.ts lib/studio/import/__tests__/import-pipeline.test.ts --silent` - record before and after; `git diff` of `web-detection.ts` shows the two existing branches untouched; `git status` shows only files this brief needs.

## Final message
Plain English: files added / changed and why each; how the live decision answer is obtained despite the shadow default; the exact diff size in `web-detection.ts` and `section-aggregation.ts`; test counts before and after; what can only be verified with paid calls (the orchestrator will run those through the lab); anything in this brief you judged unnecessary and did not do, with the reason.
