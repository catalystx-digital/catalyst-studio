# Brief M3: the lab measures the real implementation, and its own copy is deleted

You are a coder with one bounded task. You are not an orchestrator: do not run any engine, preflight or front-door tooling. Do not commit, stage, push or branch. No internet and no paid calls from anything you run (dry runs and fake clients only - the orchestrator runs the paid commands). Never read `.env*` files. Read `docs/import/block-detection-plan.md` (milestone M3) and the lab's `README.md` / `RUNBOOK.md` first.

The owner's standing rules: no over-engineering, no slop, deliver exactly what is asked - no more, no less; clean up after yourself; no dead code; as simple as possible. If you need a production function that is not exported, add `export` - never copy production logic. Memory discipline: at most one Node process at a time, Jest with `--runInBand --forceExit` one path at a time, leave no process running.

## Why
The block-based path now exists in production code (`lib/studio/import/detection/blocks/`, switched on with `IMPORT_DETECTION_HARNESS=blocks`). The lab (`scripts/experiments/import-lab/`) still carries its own earlier copy of the same logic (`block-proposal.ts`, the block part of `propose-blocks.ts`, `block-input.ts`, `picks.ts`, `extract-block.ts`, the `jev-then-llm-blocks` part of `run-arm.ts`, `arm-runtime.ts`, and `production-access.ts`, which reaches production internals by rewriting source text). Two copies will drift, and only the production one matters. The lab must measure the production code.

## Work
1. New arm `blocks-production`: for a saved page, run production detection with the `blocks` harness against the saved snapshot - the same replay mechanism `detect.ts` already uses for the old path (web tools served from disk; model and decision calls recorded one file per call with request, reply, usage, cost, latency, status). It writes the arm contract the scorer and summary already read (`components.json`, `run.json` with wall-clock seconds, calls, retries, failures, the harness settings used, and `calls/`). The repair step is not run (production skips it for this harness). `--dry-run` plans without calling. If `detect.ts` can do this with a `--harness blocks` option more simply than a new file, do that instead and say so.
2. The block cutter needs the page's stylesheets and images from the network at render time, exactly as production does; everything else is replayed from disk. State this in the RUNBOOK (INTERNET + PAID).
3. Delete the lab's duplicate implementation: every file and function that exists only for the lab's own `jev-then-llm-blocks`, `oracle-blocks`, `oracle-type-today-rules` arms and for `jev-pick`'s private evidence and picking code, where production now provides the same thing. Keep what the audit still needs and production does not provide: snapshot, replay, the old-path run and repair arms (they are history until milestone M6 removes the old path), block proposal FOR LABELLING ONLY if the review page still depends on the lab's screenshot and child-candidate data (if production's cutter can supply what the review page needs, use it and delete the lab's), label drafting, review server, scorer, family scoring, `eval.ts`, `summary.ts`, the page list. `production-access.ts` must disappear entirely or shrink to plain imports - no source rewriting.
4. `jev-pick` (picking accuracy against the answer sheet, including family mode) stays as a measurement but must call production's question and evidence code rather than a private copy.
5. `eval.ts arms` accepts the new arm; `summary.ts` shows it beside the historical arms and labels removed arms as removed, as it already does.
6. Tests: keep tests of kept behaviour, delete tests of deleted code, add tests for the new arm with fake clients (contract written, failures recorded, dry run makes no call). README / RUNBOOK rewritten to match. `briefs/README.md`: one line noting that from this point the lab measures production code and which lab files were removed.

## Verify
`npm run typecheck` 0 errors; `SKIP_DB_SETUP=true npx jest scripts/experiments/import-lab --runInBand --forceExit` passes (browser tests need `CHROMIUM_EXECUTABLE_PATH`, see README); `eval.ts summary` regenerates offline with unchanged figures for every historical arm (compare before and after); a dry run of the new arm on one saved page plans the expected number of decision and fill calls; a search for each deleted file name and exported symbol returns nothing outside `briefs/`.

## Final message
Plain English: files and lines before and after; what was deleted and what was kept and why; the exact PAID commands for the orchestrator (two runs of the new arm on the ten labelled pages, with the settings production uses by default); anything you could not verify offline.
