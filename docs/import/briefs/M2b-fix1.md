# Brief M2b fix round 1: independent review findings

You are a coder with one bounded task. Not an orchestrator. Do not commit, stage, push or branch. No internet, no paid calls, never read `.env*` files (editing `.env.example` is allowed). The uncommitted M2b work is in this working copy; `docs/import/briefs/M2b-pick-fill-wire.md` is the original brief. An independent review confirmed the old path is provably unaffected and nothing in the brief is missing. Fix exactly the items below - nothing else. The owner's standard: no over-engineering, no slop, no over- or under-delivery, no dead code, as simple as possible. Memory discipline: at most one Node process at a time, Jest with `--runInBand --forceExit` one path at a time, leave no process running.

## Must fix
1. **Wrong text shown to the model.** `lib/studio/import/detection/blocks/block-harness.ts` (around lines 100-102): when a block's pick fell back (source is not the model), the page outline given to every other block names `allowedTypes[0]` - an arbitrary production candidate. The measured lab behaviour (`scripts/experiments/import-lab/run-arm.ts`) writes `unavailable`. Do the same.
2. **Option text differs from what was measured.** `lib/studio/decisions/questions/import-block.ts` (around lines 20-22) takes the FIRST catalogue description of a type; the lab (`picks.ts`) joined duplicate descriptions with ` / `. One type is listed twice in the manifest. Join distinct descriptions the same way.
3. **A lost safety cap.** The per-page cap `DetectionConfig.maxSectionTasks` is bypassed for blocks (`block-harness.ts` around line 59), so a page cut into hundreds of blocks makes hundreds of paid calls. Apply the same guard to the number of blocks and fail the page above it with the message style the section path uses.
4. **Evidence the lab had and production lost.** The lab's "Repeated child groups" counts came from the RENDERED page with computed visibility (`scripts/experiments/import-lab/propose-blocks.ts`, around lines 110-115) and were stored on each block; `block-pick.ts` (around lines 97-108) now recomputes them from the static html tree, which differs on script-built grids - and repetition is what separates a card grid or feed from plain text. Carry the counts out of the cutter: compute them in the one geometry evaluate `renderAndCut` already performs (`block-cutter.ts`), store them on the block, and have `block-pick.ts` use them. Delete the static recomputation.

## Should fix
5. **Duplicate visibility logic.** `block-pick.ts` (around lines 49-65) re-implements the hidden/skip test that `traverseToNodes` already applies, and `bgImageMap` / `bodyFallback` sit on `BlockInput` only to feed it. Make `buildBlockInput` return the already-filtered tree (as the lab did), drop those two fields from `BlockInput` and delete `visible()` and its index pairing.
6. **Running the whole section planner to read one task.** `block-pick.ts` (around lines 16-23) builds fake sections and calls the full plan builder per block, and `block-extract.ts` (around line 57) calls it again. Export the two small functions it needs from `lib/studio/import/detection/section-plan.ts` (role for a section, candidates for a role), build the one task directly, and pass role/intent through instead of recomputing.
7. **A flag whose meaning became false.** `lib/studio/import/detection/section-aggregation.ts` (around line 66) now sets `requiredSectionEmpty = true` on block artifacts that DO have components, which makes `web-detection.ts` re-save them. Do not set it on non-empty block artifacts; keep section behaviour exactly as before. Hoist the repeated `startsWith('block:')` into one local.
8. **Bloated checkpoints.** `block-harness.ts` (around line 197) saves the full answer (a distribution over every component type) plus an issues array in which `block-pick.ts` (around lines 81-83) pushes one "indentation capped" string per deep node. Save only what resume needs (allowed types, source, top choices); record each distinct issue once.
9. **Small slop.** `blocks.indexOf(block)` / `tasks.indexOf(task)` inside concurrent workers -> map with the index; the unreachable `'Extraction exhausted'` in `block-extract.ts` (around line 221); `timeout: STALL_TIMEOUT_MS` passed to the client while the same timeout is raced manually (keep one mechanism - whichever actually aborts the HTTP request); `originalBytes` and `summarizedBytes` carrying the same number (around `block-harness.ts:210`); literals `180` / `14000` repeated in messages beside the constants that hold them; `{ ...params, ... }` at `web-detection.ts` (around line 1811) forwarding fields the harness does not declare - pass what it takes; `new URL(finalUrl).origin` unguarded in `block-harness.ts` (around line 146) where the section path falls back to `'unknown'`; `.env.example` sets `IMPORT_DETECTION_HARNESS=section` live while its neighbours are commented out - comment it like them.

Update or add tests only where behaviour changes (items 1-4, 7, 8); keep the rest of the suite as it is.

## Verify
`npm run typecheck` 0 errors; `SKIP_DB_SETUP=true npx jest lib/studio/import/detection/blocks --runInBand --forceExit` and `... lib/studio/decisions --runInBand --forceExit` and `... lib/studio/import/detection/__tests__/section-aggregation.test.ts ...` and `... lib/studio/import/__tests__/web-detection.test.ts ...` and `... lib/studio/import/services/__tests__/import-result-handler.test.ts ...` - one at a time - all pass with counts reported; `git diff -w lib/studio/import/web-detection.ts` still shows the two existing branches unchanged. `git status` shows only files these fixes need.

## Final message
Plain English, one line per item: done or not done and why; lines of `block-pick.ts`, `block-extract.ts`, `block-harness.ts` before and after.
