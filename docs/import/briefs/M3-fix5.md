# Brief M3 fix round 5 (last): three defects that drop blocks, found in measured runs

You are a coder with one bounded task. Not an orchestrator. Do not commit, stage, push or branch. No internet, no paid calls, never read `.env*` files. Memory discipline: one Node process at a time, Jest with `--runInBand --forceExit`, leave no process running. The owner's standard: no over-engineering, no slop, smallest correct change, no dead code. Fix exactly these three; nothing else.

Evidence comes from two measured runs of the `blocks` harness on 20 real pages. Saved runs you may READ (git-ignored; never copy site names, URLs or page text into code or tests): `.import-lab/arms/<slug>/blocks-production/m3-r1|m3-r2/` - `run.json` lists each failure with its block key, `calls/` holds each request and raw reply.

1. **The missing-`pageId` fix was put in the wrong place (my brief's fault).** Round 4 added `normalizeInternalLinkPageId` to `nav-normalizers.ts`, which fixed navigation bars. But the same rejection still drops other components: `feature-list: items.N.link.href.pageId:invalid_type`, `card-grid: cards.N.href.pageId:invalid_type`, and more. The fill model omits `pageId` on internal links everywhere, and `pageId` is always derivable from `path`. Do it ONCE, generically, on the detection path before validation: walk the parsed answer and, for any object with `type: "internal"`, a string `path`, and a missing or empty `pageId`, set `pageId` from the path with the existing function. Then REMOVE the navigation-only helper added in round 4 so there are not two mechanisms (keep its tests' intent by asserting navigation links through the generic step). Links that already have a `pageId`, external links and anchors are untouched.
2. **A crash inside our own code.** One block failed with `Cannot read properties of undefined (reading '0')` (run m3-r1 of one page, block key `block:6` - find it by searching the saved `run.json` files for that message). Reproduce it from the saved call for that block, find the line, and fix the cause - an unguarded index on something the model may legitimately omit. A model answer must never be able to crash the block; it can only fail validation. Add the test that reproduces it.
3. **A valid answer followed by stray text is thrown away.** Two blocks failed with `Unexpected non-whitespace character after JSON at position N`: the model wrote a complete JSON object and then more characters. The importer already has logic for incomplete JSON (`detectIncompleteJson`, `salvageTruncatedJson`); check whether the section path tolerates trailing text after a complete top-level JSON value and the block path does not, and make the block path parse the first complete top-level JSON value the same way the section path does. If the section path does not tolerate it either, make the smallest change in the shared place. Do not "repair" malformed JSON beyond ignoring trailing text after a complete value.

Not in scope: `confidence must be a finite number`, unknown fields on contact blocks, missing body on text blocks, enum values, image sources, the five copies of `pageIdFromPath`.

Verify: `npm run typecheck` 0 errors; `SKIP_DB_SETUP=true npx jest lib/studio/import/detection --runInBand --forceExit --silent` (same 4 pre-existing failures, no new ones), `... lib/studio/import/services/page-builder ...`, `... lib/studio/import/detection/blocks ...` with `CHROMIUM_EXECUTABLE_PATH=C:/Users/Admin/AppData/Local/ms-playwright/chromium_headless_shell-1234/chrome-headless-shell-win64/chrome-headless-shell.exe` - one at a time, counts reported.

Final message: plain English, one paragraph per item: cause found (file:line), the change, the test.
