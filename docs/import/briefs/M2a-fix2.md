# Brief M2a fix round 2: independent review findings

You are a coder with one bounded task. Not an orchestrator. Do not commit, stage, push or branch. No internet, no paid calls, never read `.env*` files. The uncommitted M2a work is in this working copy. Fix exactly the items below - nothing else. The owner's standard: no over-engineering, no slop, no over- or under-delivery, no dead code, as simple as possible. Memory discipline: one Node process at a time, Jest with `--runInBand --forceExit`, leave no process running.

1. **Duplicated production logic.** `lib/studio/import/detection/blocks/block-input.ts` (around lines 73-80) re-types the navigation token list that already exists in `shouldSkipBodyFallbackMainNode` in `lib/studio/import/services/web-tools.ts` (around line 551). There was never a limit on the number of exports - the rule is: export the production function you would otherwise copy. Add `export` to that function, import it, delete the local copy, and call it with the production signature (including `tag`).
2. **Tests that test the test.** In `block-cutter.test.ts` the local helper `proposeFromGeometry` builds the "probably under-cut" strings itself, so the assertions around lines 106 and 138-139 check the helper, not production. Assert on the block's `oversized` flag produced by production code instead and remove the string-building from the helper.
3. **A test guarding deleted behaviour.** `block-cutter.test.ts` around lines 150-154 asserts that no `<base>` element is injected; that feature was dropped in the port, so the test cannot fail. Delete it.
4. **Exports nothing imports.** In `block-cutter.ts` (around lines 93-114 and 444-450) 15 constants are exported and only `TALL_BLOCK` has an importer. Remove `export` from the others. `COLUMN_LAYOUT_TOLERANCE` stays a named constant.
5. **A leftover wrapper.** `block-cutter.ts` around lines 86, 412 and 580-583: `evidence?: Pick<Block, 'anchor' | 'anchorResolved'>` is a husk of a lab structure and `anchorResolved` is just `!!anchor`. Put `anchor?: Anchor | null` on `Geometry`, set it in `attachAnchors`, delete the wrapper and derive resolved-ness from the anchor.
6. **Style.** `block-cutter.ts` around lines 59, 69, 135-137, 413, 415, 424 use comma-chained multi-statement one-liners carried over from the lab. The neighbouring production files use one statement per line; rewrite those lines to match. Do not restyle anything else.

Verify: `npm run typecheck` 0 errors; each test file under `lib/studio/import/detection/blocks/` passes (browser test with `CHROMIUM_EXECUTABLE_PATH=C:/Users/Admin/AppData/Local/ms-playwright/chromium_headless_shell-1234/chrome-headless-shell-win64/chrome-headless-shell.exe`); `SKIP_DB_SETUP=true npx jest lib/studio/import/services/__tests__ --runInBand --forceExit --silent` unchanged (21 passing before). `git status` shows only files these fixes need.

Final message: plain English, one line per item - done or not done and why.
