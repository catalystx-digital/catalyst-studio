# Brief M1 fix round 1: review findings on the headless-browser change

You are a coder with one bounded task. Not an orchestrator. Do not commit, stage, push or branch. No internet, no paid calls, never read `.env*` files (editing `.env.example` is allowed). The uncommitted M1 change is in this working copy; `docs/import/briefs/M1-browser.md` is the original brief. An independent review found the items below. Fix exactly these - nothing else. The owner's standard: no over-engineering, no slop, no over- or under-delivery, no dead code, as simple as possible.

1. **Lab vocabulary in production.** `lib/studio/design-system/dom-probe/launch-headless-chromium.ts` reads `IMPORT_LAB_CHROMIUM_EXECUTABLE`, a name that belongs to the experiment folder. Rename to `CHROMIUM_EXECUTABLE_PATH` there and in its test; add ONE commented line for it to `.env.example` (local development only: path to an installed Chromium when Playwright's own browser is not installed). Then make the experiment lab use the same variable so there is one name, not two: `scripts/experiments/import-lab/lab-browser.ts` and every mention in the lab's README, RUNBOOK and tests.
2. **Error handling is over-built.** In `launch-headless-chromium.ts` remove the nested ternary, both regular expressions that parse Playwright's error text, the reassigned `source`, and the `chromium.executablePath()` call inside `catch` (it can throw and destroy the one clear error). Decide `source` and `path` ONCE before `chromium.launch`, from the known inputs (launch-option override, `CHROMIUM_EXECUTABLE_PATH`, serverless package, Playwright default); in `catch` only build the message from them plus the cause. Keep the message plain: what was tried, what to do.
3. **Timeout constant.** The launcher hardcodes `60000` while `service.ts` defines `DEFAULT_TIMEOUT_MS = 60000`. Use one constant (export it from where it lives and import it), no duplicate.
4. **Tests.** Cut the launcher test to what the brief asked: the three path-resolution cases (serverless, explicit path, default) plus ONE launch-failure case asserting the message names the source and the action. Delete the rest, including the case asserting `console.warn` (that is another module's behaviour) and anything that existed to exercise the regular expressions.
5. **Docker image size.** Only the headless shell is ever launched. Use `npx playwright-core install --with-deps --only-shell chromium`. Remove the redundant `chmod -R a+rX` if root's default permissions already make `/ms-playwright` readable and executable for the `nextjs` user - say in your final message how you know.
6. **CI.** The job that now builds the image has `timeout-minutes: 20` and already runs an install, seeding and a dev server; a full production image build on top will likely exceed it. Raise it to 40. Add `--rootDir .` to the `npx tsc` command so the emitted path `headless-check/scripts/check-headless-browser.js` does not depend on inference.

Verify: `npm run typecheck` 0 errors; `SKIP_DB_SETUP=true npx jest lib/studio/design-system/dom-probe/__tests__/launch-headless-chromium.test.ts` passes; the lab suite still passes with the renamed variable (`SKIP_DB_SETUP=true npx jest scripts/experiments/import-lab --runInBand`, with `CHROMIUM_EXECUTABLE_PATH=C:/Users/Admin/AppData/Local/ms-playwright/chromium_headless_shell-1234/chrome-headless-shell-win64/chrome-headless-shell.exe`); `npx tsx scripts/check-headless-browser.ts` with that variable prints the four blocks; a search for `IMPORT_LAB_CHROMIUM_EXECUTABLE` returns nothing outside `scripts/experiments/import-lab/briefs/` (historical briefs stay as written). `git status` shows only files these fixes need.

Final message: plain English, one line per item - done or not done and why.
