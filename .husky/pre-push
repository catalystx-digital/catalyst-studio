#!/usr/bin/env sh

# Pre-push checks are temporarily disabled due to timeout issues
# To re-enable, uncomment the lines below

# echo "🔍 Running pre-push checks..."
# echo "⚠️  Pre-push checks are currently disabled for development speed"
# echo "✅ Skipping pre-push checks - pushing directly"

# Original checks (disabled):
# - Bundle size checking (npm run build)
# - Test coverage (npm run test:coverage:ci)
# 
# To run these checks manually:
# npm run build
# npm run test:coverage:ci

exit 0