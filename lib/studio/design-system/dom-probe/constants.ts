export const DOM_PROBE_VERSION = '0.1.0'
// Note: This is joined with os.tmpdir() on Vercel (which is /tmp) or process.cwd() locally.
// Do NOT include 'tmp/' prefix here - the base directory already provides the correct root.
export const DOM_PROBE_TMP_ROOT = 'vision-dom-probe'
export const DOM_PROBE_NAMESPACE = 'vision-dom-probe'
